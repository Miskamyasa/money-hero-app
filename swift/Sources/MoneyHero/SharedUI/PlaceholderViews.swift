import SwiftUI

struct PortfolioPlaceholderView: View {
    var body: some View {
        Color.clear
    }
}

struct SettingsPlaceholderView: View {
    var body: some View {
        Color.clear
    }
}
